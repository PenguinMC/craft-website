{
  "name": "craft-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static marketing site for CRAFT Flight Training. Deployed to Vercel via auto-deploy from main.",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0"
  }
}
